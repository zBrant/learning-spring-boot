server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zBrant/learning-spring-boot
          default-label: main
          search-paths:
            - 'config-server-files/greeting-service*'